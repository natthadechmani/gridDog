# Copy this file to secrets.env and fill in real values.
# secrets.env is gitignored — never commit it.

# Postgres + MongoDB password (must match what you set in terraform.tfvars
# for the VM stack if you want consistent test data).
db_password=griddog

# dd_api_key will be added when the Datadog observability plan ships.
