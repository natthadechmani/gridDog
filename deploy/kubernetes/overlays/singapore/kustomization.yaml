apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Singapore overlay: ap-southeast-1 (ap-southeast-1b)
# Cost-saving configuration: all Deployments scaled to 1 replica.

bases:
  - ../../base

namePrefix: sg-

commonLabels:
  region: ap-southeast-1
  az: ap-southeast-1b

# Scale all Deployments down to 1 replica for cost savings in this region.
patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: backend
        namespace: griddog
      spec:
        replicas: 1
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: java-service
        namespace: griddog
      spec:
        replicas: 1
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: express-service
        namespace: griddog
      spec:
        replicas: 1
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: frontend
        namespace: griddog
      spec:
        replicas: 1

# Override REGION to reflect the Singapore AWS region.
configMapGenerator:
  - name: griddog-config
    namespace: griddog
    behavior: merge
    literals:
      - REGION=ap-southeast-1
